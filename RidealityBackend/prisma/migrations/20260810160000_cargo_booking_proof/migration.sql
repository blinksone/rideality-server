-- Cargo booking + proof (also applied via prisma db push)
-- BookingType / proof enums, ride cargo columns, driver service_modes, vehicle capacity, cargo_proofs

-- Enums created by Prisma push if missing
